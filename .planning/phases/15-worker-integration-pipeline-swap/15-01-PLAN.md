---
phase: 15-worker-integration-pipeline-swap
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/audioQuality.ts
  - src/lib/__tests__/audioQuality.test.ts
autonomous: true
requirements: [CONF-03, CONF-04]

must_haves:
  truths:
    - "Silence detection returns a warning when RMS is below -50dB threshold (0.003)"
    - "Clipping detection returns a warning when >0.5% of samples are at +/-1.0"
    - "Normal audio with moderate levels and no clipping returns zero warnings"
    - "Empty audio (zero-length array) returns zero warnings"
  artifacts:
    - path: "src/lib/audioQuality.ts"
      provides: "detectAudioWarnings pure function and AudioWarning type"
      exports: ["detectAudioWarnings", "AudioWarning"]
    - path: "src/lib/__tests__/audioQuality.test.ts"
      provides: "Unit tests for silence and clipping detection"
      min_lines: 40
  key_links:
    - from: "src/lib/audioQuality.ts"
      to: "Float32Array PCM input"
      via: "detectAudioWarnings(pcm) pure function"
      pattern: "detectAudioWarnings"
---

<objective>
Create the audio quality detection module with TDD: pure functions that analyze Float32Array PCM for silence and clipping, returning typed AudioWarning arrays.

Purpose: CONF-03 (silence detection) and CONF-04 (clipping detection) require pre-sync audio analysis that runs on the main thread before the worker is initialized (per D-06). This module is a standalone pure-function library with no external dependencies.

Output: `src/lib/audioQuality.ts` (production) and `src/lib/__tests__/audioQuality.test.ts` (tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-worker-integration-pipeline-swap/15-CONTEXT.md
@.planning/phases/15-worker-integration-pipeline-swap/15-RESEARCH.md

<interfaces>
<!-- AudioWarning type created by this plan, consumed by Plan 03 UI integration -->

From src/lib/audioQuality.ts (TO BE CREATED):
```typescript
export interface AudioWarning {
  type: 'silence' | 'clipping' | 'low-confidence';
  message: string;
}

export function detectAudioWarnings(pcm: Float32Array): AudioWarning[];
```

<!-- No existing interfaces needed -- this is a leaf module with zero imports -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for audio quality detection (RED)</name>
  <files>src/lib/__tests__/audioQuality.test.ts</files>
  <read_first>
    - src/lib/__tests__/fftEngine.test.ts (for project test patterns and synthetic signal helpers)
    - .planning/phases/15-worker-integration-pipeline-swap/15-RESEARCH.md (section: Audio Quality Detection, Recommended Thresholds)
  </read_first>
  <behavior>
    - Test: silent signal (all zeros) returns a 'silence' warning with message containing "silent or near-silent"
    - Test: near-silent signal (RMS < 0.003, e.g. amplitude 0.001) returns 'silence' warning
    - Test: normal audio (RMS ~0.1, typical speech) returns NO silence warning
    - Test: heavily clipped signal (>0.5% samples at +/-1.0) returns 'clipping' warning with message containing "clipping distortion"
    - Test: signal with occasional peaks (< 0.5% at +/-1.0) returns NO clipping warning
    - Test: signal that is BOTH silent and clipped returns BOTH warnings
    - Test: empty Float32Array (length 0) returns empty warnings array
    - Test: AudioWarning type has 'type' and 'message' fields
  </behavior>
  <action>
    Create `src/lib/__tests__/audioQuality.test.ts` with comprehensive tests for `detectAudioWarnings()`.

    Use synthetic Float32Array signals:
    - Silent: `new Float32Array(16000)` (all zeros)
    - Near-silent: fill with amplitude 0.001 (RMS = 0.001, well below 0.003 threshold)
    - Normal: fill with amplitude 0.1 (RMS ~0.07, well above threshold)
    - Clipped: fill 1% of samples with 1.0 or -1.0 (above 0.5% threshold)
    - Lightly peaked: fill 0.1% of samples with 1.0 (below 0.5% threshold)
    - Empty: `new Float32Array(0)`

    Import from `../audioQuality.ts` (file doesn't exist yet -- tests MUST fail).

    Thresholds per research (Claude's Discretion):
    - Silence RMS threshold: 0.003 (~-50dB)
    - Clipping ratio threshold: 0.005 (0.5%)
    - Clip boundary: |sample| >= 0.999

    Do NOT create audioQuality.ts yet. Tests must fail (RED phase).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Tyler/OneDrive/Projects/sync-multi-cam && npx vitest run src/lib/__tests__/audioQuality.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/__tests__/audioQuality.test.ts` exists with at least 7 test cases
    - Running tests FAILS because `src/lib/audioQuality.ts` does not exist yet
    - Tests import `detectAudioWarnings` and `AudioWarning` from `../audioQuality.ts`
    - grep confirms: `grep -c "it\|test(" src/lib/__tests__/audioQuality.test.ts` returns >= 7
  </acceptance_criteria>
  <done>Test file exists with 7+ test cases covering silence, clipping, normal, edge cases. All tests fail (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement audioQuality.ts to pass all tests (GREEN)</name>
  <files>src/lib/audioQuality.ts</files>
  <read_first>
    - src/lib/__tests__/audioQuality.test.ts (the tests just written -- implementation must satisfy them)
    - .planning/phases/15-worker-integration-pipeline-swap/15-RESEARCH.md (section: Code Examples > Audio Quality Detection)
  </read_first>
  <behavior>
    - detectAudioWarnings(pcm) computes RMS and clip ratio in a single pass over the Float32Array
    - Returns AudioWarning[] with 'silence' type when RMS < 0.003
    - Returns AudioWarning[] with 'clipping' type when clipRatio > 0.005
    - Returns empty array for normal audio
    - Handles zero-length input gracefully
  </behavior>
  <action>
    Create `src/lib/audioQuality.ts` implementing the `detectAudioWarnings` function per D-05, D-06.

    Constants (internal, not exported):
    - `SILENCE_RMS_THRESHOLD = 0.003` (~-50dB)
    - `CLIPPING_RATIO_THRESHOLD = 0.005` (0.5%)
    - `CLIP_EPSILON = 0.001` (boundary for detecting samples at +/-1.0)

    Export:
    - `AudioWarning` interface with `type: 'silence' | 'clipping' | 'low-confidence'` and `message: string`
    - `detectAudioWarnings(pcm: Float32Array): AudioWarning[]`

    Implementation: single-pass loop computing both `sumSq` (for RMS) and `clipCount` (for clipping ratio). This is O(N) with no allocations beyond the output array.

    Warning messages per D-09 (non-blocking, "may be" language):
    - Silence: "Audio is silent or near-silent -- sync may be unreliable"
    - Clipping: "Audio has clipping distortion -- sync may be affected"

    After implementation, run tests to confirm GREEN.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Tyler/OneDrive/Projects/sync-multi-cam && npx vitest run src/lib/__tests__/audioQuality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/audioQuality.ts` exists
    - All audioQuality tests pass (GREEN)
    - `grep -c "export" src/lib/audioQuality.ts` shows at least 2 exports (AudioWarning, detectAudioWarnings)
    - `grep "SILENCE_RMS_THRESHOLD" src/lib/audioQuality.ts` matches (threshold constant exists)
    - `grep "CLIPPING_RATIO_THRESHOLD" src/lib/audioQuality.ts` matches (threshold constant exists)
    - Full test suite still passes: `npx vitest run` (no regressions)
  </acceptance_criteria>
  <done>All audioQuality tests pass. detectAudioWarnings correctly identifies silence, clipping, and normal audio. Full test suite green.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/__tests__/audioQuality.test.ts` -- all tests pass
- `npx vitest run` -- full suite passes (no regressions)
- `grep "detectAudioWarnings" src/lib/audioQuality.ts` -- function exported
- `grep "AudioWarning" src/lib/audioQuality.ts` -- type exported
</verification>

<success_criteria>
1. `src/lib/audioQuality.ts` exports `AudioWarning` type and `detectAudioWarnings()` function
2. `src/lib/__tests__/audioQuality.test.ts` has 7+ tests covering silence, clipping, normal, and edge cases
3. All tests pass
4. Full test suite has zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/15-worker-integration-pipeline-swap/15-01-SUMMARY.md`
</output>
