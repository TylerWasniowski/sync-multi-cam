---
phase: 08-composite-export
task: 2
total_tasks: 2
status: debugging
last_updated: 2026-03-03T20:15:50.177Z
---

<current_state>
Phase 8 WebCodecs export rework: Plan 08-01 (pipeline infrastructure) is committed. Plan 08-02 (UI wiring) Task 1 is committed. Task 2 is a human-verify checkpoint — we're stuck debugging why the OLD FFmpeg WASM export still runs instead of the new WebCodecs pipeline.

The source code on disk IS the new WebCodecs version (verified: ExportPanel imports from new exportComposite.ts which spawns a Web Worker). But the browser console shows the old FFmpeg xstack/libx264 pipeline running when Export is clicked. Clearing Vite cache (node_modules/.vite) and restarting the dev server did NOT fix it.
</current_state>

<completed_work>

- Plan 08-01 Task 1: Install mediabunny, update types, remove old export args — Done (commit d97a772)
- Plan 08-01 Task 2: Build exportWorker.ts (WebCodecs pipeline) + rewrite exportComposite.ts (main-thread API) — Done (commit ef67051)
- Plan 08-01 SUMMARY committed (commit 431a715)
- Plan 08-02 Task 1: Rewrite ExportPanel with cancel/WebCodecs check, update PlaybackSection props — Done (commit 5f9ad0a)
- Plan 08-02 Task 2: Human-verify checkpoint — IN PROGRESS (blocked on debugging)
- Fixed CanvasSource config: removed invalid hardwareAcceleration/latencyMode fields, removed invalid frameRate metadata — uncommitted in exportWorker.ts
- Added console.log debug statements to exportWorker.ts — uncommitted
</completed_work>

<remaining_work>

- Debug why old FFmpeg export path still runs despite new code on disk
- Complete human verification of WebCodecs export (Task 2 checkpoint)
- Phase verification and roadmap update
</remaining_work>

<decisions_made>

- WebCodecs + OffscreenCanvas + Mediabunny (not FFmpeg WASM) for export
- Entire pipeline in a Web Worker for responsive UI
- Mediabunny v1.35.1 for demux (HEVC) + mux (MP4 H.264+AAC)
- CanvasSource for H.264 encoding, VideoSampleSink for HEVC decoding
- Cancel button added, WebCodecs-only (no fallback), FFmpeg WASM to be fully removed
- Keep resolution presets (720p/1080p/4K), respect mute state from UI
</decisions_made>

<blockers>
- RESOLVED: Old FFmpeg export was running because sandbox silently blocked kill signals — server never actually restarted. Fixed by using dangerouslyDisableSandbox for kill commands.
- NEW ERROR: WebCodecs pipeline now runs but Mediabunny Input throws "Input has an unsupported or unrecognizable format." when trying to demux the source files. The files are HEVC MP4s (some trimmed by videoTrimmer via FFmpeg Lavf59.27.100). Likely the BlobSource or MP4 format detection needs fixing — check if files need to be passed as original File objects (not trimmed copies) or if the MP4 InputFormat needs different config.
</blockers>

<context>
The old FFmpeg WASM approach (xstack + libx264) was too slow for 3x 1080p HEVC. We rewrote the entire export to use WebCodecs API with hardware-accelerated encoding via Mediabunny. The new code compiles without TypeScript errors and the API matches mediabunny v1.35.1 types (verified: Input, VideoSampleSink, CanvasSource, AudioBufferSink, AudioBufferSource, Output, Mp4OutputFormat, BufferTarget all have correct signatures).

Key mediabunny API findings:
- VideoEncodingConfig does NOT have hardwareAcceleration or latencyMode (fixed, uncommitted)
- VideoTrackMetadata does NOT have frameRate (fixed, uncommitted)
- AudioBufferSink.buffers() yields WrappedAudioBuffer with {buffer, timestamp, duration} — code correctly destructures
- VideoSample has .draw(ctx, x, y, w, h) and .close() — code uses both correctly

Next debugging steps:
1. Have user clear ALL site data (DevTools → Application → Storage → Clear site data)
2. If still broken, check if there's a service worker registered
3. If still broken, check if Vite is somehow serving a cached pre-built bundle from dist/
4. Consider: maybe the ExportPanel on screen is actually a DIFFERENT component than what's in source (stale HMR state)
</context>

<next_action>
Start with: Ask user if "Clear site data" in DevTools fixed the stale cache. If not, check for service workers and stale dist/ artifacts. Once the new WebCodecs pipeline actually runs in browser, debug any runtime errors using the console.log statements added to exportWorker.ts. Then complete the human-verify checkpoint for plan 08-02.
</next_action>
