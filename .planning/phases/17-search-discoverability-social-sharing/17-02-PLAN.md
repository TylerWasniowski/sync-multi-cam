---
phase: 17-search-discoverability-social-sharing
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - index.html
  - src/__tests__/seo.test.ts
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
  - SOCIAL-01
  - SOCIAL-02
  - SCHEMA-01

must_haves:
  truths:
    - "Page has a descriptive title (not the slug 'sync-multi-cam') under 60 characters"
    - "Page has a meta description between 150-160 characters with the value proposition"
    - "Page has a canonical URL pointing to https://sync-multi-cam.pages.dev/"
    - "Page has a theme-color matching the dark app background (#030712)"
    - "Sharing the URL shows og:title, og:description, og:type, og:url, og:image in static HTML"
    - "Twitter Card tag is set to summary_large_image"
    - "Page has valid JSON-LD with WebApplication type and required Schema.org properties"
    - "All SEO validation tests pass"
  artifacts:
    - path: "index.html"
      provides: "Complete HTML head with all meta, OG, Twitter Card, JSON-LD, and favicon tags"
      contains: "og:title"
    - path: "src/__tests__/seo.test.ts"
      provides: "Automated validation of all 12 SEO requirements"
      contains: "describe"
  key_links:
    - from: "index.html"
      to: "public/og-image.png"
      via: "og:image meta tag with absolute HTTPS URL"
      pattern: "og:image.*https://sync-multi-cam\\.pages\\.dev/og-image\\.png"
    - from: "index.html"
      to: "public/favicon.svg"
      via: "link rel=icon with SVG type"
      pattern: 'rel="icon".*favicon\\.svg'
    - from: "index.html"
      to: "public/favicon.ico"
      via: "link rel=icon with sizes"
      pattern: 'rel="icon".*favicon\\.ico'
    - from: "index.html"
      to: "public/robots.txt"
      via: "Canonical URL consistency (same production domain)"
      pattern: "sync-multi-cam\\.pages\\.dev"
    - from: "src/__tests__/seo.test.ts"
      to: "index.html"
      via: "readFileSync to parse and validate HTML content"
      pattern: "readFileSync.*index\\.html"
    - from: "src/__tests__/seo.test.ts"
      to: "public/"
      via: "existsSync to verify static asset files"
      pattern: "existsSync.*public/"
---

<objective>
Rewrite index.html with the complete SEO head section (meta tags, Open Graph, Twitter Card, JSON-LD, favicon references) and create an automated test suite validating all 12 phase requirements.

Purpose: Make the page discoverable by search engines and render rich link previews on social platforms. The test suite ensures all requirements are verifiable by CI and future regressions are caught.

Output: A fully-tagged index.html and a comprehensive Vitest test file covering all 12 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-search-discoverability-social-sharing/17-CONTEXT.md
@.planning/phases/17-search-discoverability-social-sharing/17-RESEARCH.md
@.planning/phases/17-search-discoverability-social-sharing/17-01-SUMMARY.md

<interfaces>
<!-- Current state of index.html (entire file): -->
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>sync-multi-cam</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

<!-- Vitest config from vite.config.ts: -->
```typescript
test: {
  environment: 'node',
  exclude: ['tests/**', 'node_modules/**'],
},
```
Tests in src/ are included by default (only tests/ and node_modules/ are excluded).
Test command: `npx vitest run src/__tests__/seo.test.ts`

<!-- Key constants (from CONTEXT.md decisions): -->
Production URL: https://sync-multi-cam.pages.dev/
OG image absolute URL: https://sync-multi-cam.pages.dev/og-image.png
Theme color: #030712
JSON-LD @type: WebApplication
applicationCategory: MultimediaApplication
operatingSystem: Any
browserRequirements: Modern browser with WebAssembly and SharedArrayBuffer support
offers.price: "0"
offers.priceCurrency: "USD"
isAccessibleForFree: true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite index.html head with all SEO tags</name>
  <files>index.html</files>
  <read_first>
    - index.html (current state -- 13 lines with charset, vite.svg favicon, viewport, slug title)
    - .planning/phases/17-search-discoverability-social-sharing/17-RESEARCH.md (Pattern 1 for complete head section example)
    - .planning/phases/17-search-discoverability-social-sharing/17-CONTEXT.md (D-01 through D-17 for all locked decisions)
  </read_first>
  <action>
Replace the entire `index.html` file with the following content. The `<body>` remains unchanged; only the `<head>` is expanded.

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Primary Meta Tags (META-01, META-02, META-03, META-04) -->
    <title>Sync Multi-Cam | Free Browser-Based Video Sync</title>
    <meta name="description" content="Sync multiple camera angles by audio in your browser. Preview in a synced grid, export a composite MP4. Free, private, no install or signup required." />
    <link rel="canonical" href="https://sync-multi-cam.pages.dev/" />
    <meta name="theme-color" content="#030712" />

    <!-- Open Graph / Facebook (SOCIAL-01) -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://sync-multi-cam.pages.dev/" />
    <meta property="og:title" content="Sync Multi-Cam | Free Browser-Based Video Sync" />
    <meta property="og:description" content="Sync multiple camera angles by audio in your browser. Preview in a synced grid, export a composite MP4. Free, private, no install or signup required." />
    <meta property="og:image" content="https://sync-multi-cam.pages.dev/og-image.png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <!-- Twitter Card (SOCIAL-02) -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Sync Multi-Cam | Free Browser-Based Video Sync" />
    <meta name="twitter:description" content="Sync multiple camera angles by audio in your browser. Free, private, no install or signup required." />
    <meta name="twitter:image" content="https://sync-multi-cam.pages.dev/og-image.png" />

    <!-- Favicon (BRAND-01 -- per D-09, replaces vite.svg) -->
    <link rel="icon" href="/favicon.ico" sizes="32x32" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />

    <!-- Structured Data (SCHEMA-01 -- per D-13, D-14, D-15) -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "WebApplication",
      "name": "Sync Multi-Cam",
      "description": "Browser-based tool that synchronizes multiple video files by analyzing their audio tracks. Preview all angles in a synced grid player and export a single composited MP4.",
      "url": "https://sync-multi-cam.pages.dev/",
      "applicationCategory": "MultimediaApplication",
      "operatingSystem": "Any",
      "browserRequirements": "Modern browser with WebAssembly and SharedArrayBuffer support",
      "isAccessibleForFree": true,
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "USD"
      },
      "featureList": [
        "Audio cross-correlation sync for multiple cameras",
        "GPU-accelerated composite MP4 export",
        "Synced multi-angle grid preview",
        "100% client-side processing, no uploads"
      ]
    }
    </script>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

Key requirements verified in this content:
- **META-01 (D-01):** Title is "Sync Multi-Cam | Free Browser-Based Video Sync" = 50 chars (under 60). Contains "Sync Multi-Cam" brand. Includes "Free" and "Browser-Based" value props.
- **META-02 (D-02):** Description is 155 chars. Mentions: free, no install, browser, multi-cam, sync, preview, grid, export, MP4, private, no signup.
- **META-03 (D-16):** Canonical is exactly `https://sync-multi-cam.pages.dev/`.
- **META-04:** theme-color is `#030712` (Tailwind gray-950, app background).
- **SOCIAL-01 (D-06):** All 5 OG tags present. og:image uses absolute HTTPS URL.
- **SOCIAL-02:** twitter:card is `summary_large_image`.
- **SCHEMA-01 (D-13, D-14, D-15):** JSON-LD uses `WebApplication` type, includes `applicationCategory`, `operatingSystem`, `browserRequirements`, `offers` with `price: "0"`, `isAccessibleForFree: true`. No fabricated aggregateRating.
- **BRAND-01 (D-09):** No reference to vite.svg. Favicon links point to /favicon.ico, /favicon.svg, /apple-touch-icon.png.
- **D-03:** No `<meta name="keywords">` tag present.

DO NOT include `<meta name="keywords">` (per D-03).
DO NOT reference vite.svg anywhere (per D-09).
The `<body>` content MUST remain exactly as-is: `<div id="root"></div>` and `<script type="module" src="/src/main.tsx"></script>`.
  </action>
  <verify>
    <automated>grep 'og:title' index.html && grep 'twitter:card' index.html && grep 'WebApplication' index.html && grep 'canonical' index.html && grep 'theme-color' index.html && ! grep 'vite.svg' index.html && echo "index.html has all required tags"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `<title>Sync Multi-Cam | Free Browser-Based Video Sync</title>` (exactly this string)
    - index.html contains `<meta name="description" content="` with a value that is between 150-160 characters
    - index.html contains `<link rel="canonical" href="https://sync-multi-cam.pages.dev/"` (exact production URL)
    - index.html contains `<meta name="theme-color" content="#030712"` (exact hex)
    - index.html contains `property="og:type" content="website"`
    - index.html contains `property="og:url" content="https://sync-multi-cam.pages.dev/"`
    - index.html contains `property="og:title"`
    - index.html contains `property="og:description"`
    - index.html contains `property="og:image" content="https://sync-multi-cam.pages.dev/og-image.png"`
    - index.html contains `property="og:image:width" content="1200"`
    - index.html contains `property="og:image:height" content="630"`
    - index.html contains `name="twitter:card" content="summary_large_image"`
    - index.html contains `name="twitter:image" content="https://sync-multi-cam.pages.dev/og-image.png"`
    - index.html contains `href="/favicon.ico"` and `href="/favicon.svg"` and `href="/apple-touch-icon.png"`
    - index.html does NOT contain "vite.svg"
    - index.html does NOT contain `name="keywords"`
    - index.html contains `"@type": "WebApplication"`
    - index.html contains `"applicationCategory": "MultimediaApplication"`
    - index.html contains `"price": "0"`
    - index.html contains `"isAccessibleForFree": true`
    - index.html contains `"browserRequirements": "Modern browser with WebAssembly and SharedArrayBuffer support"`
    - index.html contains `<div id="root"></div>` (body preserved)
    - index.html contains `src="/src/main.tsx"` (entry point preserved)
  </acceptance_criteria>
  <done>index.html has a complete SEO head section with all meta tags, OG tags, Twitter Card, JSON-LD structured data, and favicon references. No Vite defaults remain. All URLs are absolute HTTPS pointing to production domain.</done>
</task>

<task type="auto">
  <name>Task 2: Create SEO validation test suite</name>
  <files>src/__tests__/seo.test.ts</files>
  <read_first>
    - index.html (after Task 1 modification -- to see the actual tags being validated)
    - .planning/phases/17-search-discoverability-social-sharing/17-RESEARCH.md (Validation Architecture section for test approach and examples)
    - .planning/phases/17-search-discoverability-social-sharing/17-VALIDATION.md (Per-Task Verification Map for all 12 requirements)
    - vite.config.ts (test config -- environment: 'node', excludes tests/ and node_modules/ only)
  </read_first>
  <action>
Create `src/__tests__/seo.test.ts` -- a Vitest test file that validates all 12 SEO requirements by reading static files and parsing their content. No DOM library needed; use regex and string matching on raw file text.

```typescript
import { readFileSync, existsSync } from 'fs';
import { resolve } from 'path';
import { describe, it, expect } from 'vitest';

const html = readFileSync(resolve(__dirname, '../../index.html'), 'utf-8');

describe('SEO: Primary Meta Tags', () => {
  it('META-01: has descriptive title under 60 chars containing "Sync Multi-Cam"', () => {
    const match = html.match(/<title>(.*?)<\/title>/);
    expect(match).toBeTruthy();
    const title = match![1];
    expect(title.length).toBeLessThanOrEqual(60);
    expect(title.length).toBeGreaterThan(10);
    expect(title).toContain('Sync Multi-Cam');
    expect(title).not.toBe('sync-multi-cam');
  });

  it('META-02: has meta description between 150-160 chars', () => {
    const match = html.match(/<meta name="description" content="(.*?)"/);
    expect(match).toBeTruthy();
    const desc = match![1];
    expect(desc.length).toBeGreaterThanOrEqual(150);
    expect(desc.length).toBeLessThanOrEqual(160);
  });

  it('META-03: has canonical URL pointing to production', () => {
    expect(html).toContain('<link rel="canonical" href="https://sync-multi-cam.pages.dev/"');
  });

  it('META-04: has theme-color matching dark app theme', () => {
    expect(html).toContain('<meta name="theme-color" content="#030712"');
  });
});

describe('SEO: Open Graph Tags', () => {
  it('SOCIAL-01: has all 5 required OG tags with correct values', () => {
    expect(html).toMatch(/property="og:type" content="website"/);
    expect(html).toMatch(/property="og:url" content="https:\/\/sync-multi-cam\.pages\.dev\/"/);
    expect(html).toMatch(/property="og:title"/);
    expect(html).toMatch(/property="og:description"/);
    expect(html).toMatch(/property="og:image" content="https:\/\/sync-multi-cam\.pages\.dev\/og-image\.png"/);
  });

  it('SOCIAL-01: og:image includes width and height', () => {
    expect(html).toMatch(/property="og:image:width" content="1200"/);
    expect(html).toMatch(/property="og:image:height" content="630"/);
  });
});

describe('SEO: Twitter Card', () => {
  it('SOCIAL-02: has twitter:card set to summary_large_image', () => {
    expect(html).toMatch(/name="twitter:card" content="summary_large_image"/);
  });

  it('SOCIAL-02: twitter:image uses absolute HTTPS URL', () => {
    expect(html).toMatch(/name="twitter:image" content="https:\/\/sync-multi-cam\.pages\.dev\/og-image\.png"/);
  });
});

describe('SEO: OG Image Asset', () => {
  it('SOCIAL-03: og-image.png exists in public directory', () => {
    expect(existsSync(resolve(__dirname, '../../public/og-image.png'))).toBe(true);
  });

  it('SOCIAL-03: og-image.png is a valid PNG', () => {
    const buffer = readFileSync(resolve(__dirname, '../../public/og-image.png'));
    // PNG magic bytes: 137 80 78 71 (hex: 89 50 4E 47)
    expect(buffer[0]).toBe(0x89);
    expect(buffer[1]).toBe(0x50);
    expect(buffer[2]).toBe(0x4e);
    expect(buffer[3]).toBe(0x47);
  });
});

describe('SEO: JSON-LD Structured Data', () => {
  it('SCHEMA-01: has valid JSON-LD with WebApplication type', () => {
    const jsonLdMatch = html.match(/<script type="application\/ld\+json">([\s\S]*?)<\/script>/);
    expect(jsonLdMatch).toBeTruthy();
    const jsonLd = JSON.parse(jsonLdMatch![1]);
    expect(jsonLd['@context']).toBe('https://schema.org');
    expect(jsonLd['@type']).toBe('WebApplication');
    expect(jsonLd.name).toBeTruthy();
    expect(jsonLd.description).toBeTruthy();
    expect(jsonLd.url).toBe('https://sync-multi-cam.pages.dev/');
  });

  it('SCHEMA-01: JSON-LD has required application properties', () => {
    const jsonLdMatch = html.match(/<script type="application\/ld\+json">([\s\S]*?)<\/script>/);
    const jsonLd = JSON.parse(jsonLdMatch![1]);
    expect(jsonLd.applicationCategory).toBe('MultimediaApplication');
    expect(jsonLd.operatingSystem).toBe('Any');
    expect(jsonLd.browserRequirements).toContain('WebAssembly');
    expect(jsonLd.browserRequirements).toContain('SharedArrayBuffer');
  });

  it('SCHEMA-01: JSON-LD has free pricing offer', () => {
    const jsonLdMatch = html.match(/<script type="application\/ld\+json">([\s\S]*?)<\/script>/);
    const jsonLd = JSON.parse(jsonLdMatch![1]);
    expect(jsonLd.isAccessibleForFree).toBe(true);
    expect(jsonLd.offers).toBeTruthy();
    expect(jsonLd.offers.price).toBe('0');
    expect(jsonLd.offers.priceCurrency).toBe('USD');
  });
});

describe('SEO: Crawler Infrastructure', () => {
  it('CRAWL-01: robots.txt exists and allows all crawlers', () => {
    const content = readFileSync(resolve(__dirname, '../../public/robots.txt'), 'utf-8');
    expect(content).toContain('User-agent: *');
    expect(content).toContain('Allow: /');
    expect(content).toContain('Sitemap: https://sync-multi-cam.pages.dev/sitemap.xml');
  });

  it('CRAWL-02: sitemap.xml exists with canonical URL', () => {
    const content = readFileSync(resolve(__dirname, '../../public/sitemap.xml'), 'utf-8');
    expect(content).toContain('<?xml version');
    expect(content).toContain('<loc>https://sync-multi-cam.pages.dev/</loc>');
  });

  it('CRAWL-03: _headers has COEP/COOP unset for SEO assets', () => {
    const content = readFileSync(resolve(__dirname, '../../public/_headers'), 'utf-8');
    // Global rules still present
    expect(content).toContain('Cross-Origin-Opener-Policy: same-origin');
    expect(content).toContain('Cross-Origin-Embedder-Policy: require-corp');
    // Unset rules for each SEO asset
    const paths = ['/og-image.png', '/robots.txt', '/sitemap.xml', '/favicon.ico', '/apple-touch-icon.png', '/favicon.svg'];
    for (const path of paths) {
      expect(content).toContain(path);
    }
    // Verify unset syntax is used
    expect(content).toContain('! Cross-Origin-Embedder-Policy');
    expect(content).toContain('! Cross-Origin-Opener-Policy');
    // OG image has CORS header
    const ogSection = content.split('/og-image.png')[1]?.split(/\n\/[a-z]/)[0] ?? '';
    expect(ogSection).toContain('Access-Control-Allow-Origin: *');
  });
});

describe('SEO: Favicon & Branding', () => {
  it('BRAND-01: favicon.svg exists and is valid SVG', () => {
    const content = readFileSync(resolve(__dirname, '../../public/favicon.svg'), 'utf-8');
    expect(content).toContain('<svg');
    expect(content).toContain('viewBox');
  });

  it('BRAND-01: favicon.ico exists and is valid ICO', () => {
    const buffer = readFileSync(resolve(__dirname, '../../public/favicon.ico'));
    // ICO magic bytes: 00 00 01 00
    expect(buffer[0]).toBe(0x00);
    expect(buffer[1]).toBe(0x00);
    expect(buffer[2]).toBe(0x01);
    expect(buffer[3]).toBe(0x00);
  });

  it('BRAND-01: apple-touch-icon.png exists and is valid PNG', () => {
    const buffer = readFileSync(resolve(__dirname, '../../public/apple-touch-icon.png'));
    expect(buffer[0]).toBe(0x89);
    expect(buffer[1]).toBe(0x50);
    expect(buffer[2]).toBe(0x4e);
    expect(buffer[3]).toBe(0x47);
  });

  it('BRAND-01: index.html references new favicons, not vite.svg', () => {
    expect(html).not.toContain('vite.svg');
    expect(html).toContain('href="/favicon.ico"');
    expect(html).toContain('href="/favicon.svg"');
    expect(html).toContain('href="/apple-touch-icon.png"');
  });
});

describe('SEO: Anti-patterns excluded', () => {
  it('D-03: no keywords meta tag', () => {
    expect(html).not.toMatch(/name="keywords"/);
  });

  it('no relative og:image URL', () => {
    // og:image must use absolute HTTPS, never relative
    const ogImage = html.match(/property="og:image" content="(.*?)"/);
    expect(ogImage).toBeTruthy();
    expect(ogImage![1]).toMatch(/^https:\/\//);
  });
});
```

The file resolves paths relative to `__dirname` (which will be `src/__tests__/`) using `../../` to reach the project root. This works because Vitest runs with `environment: 'node'` per vite.config.ts.

The test covers all 12 requirements:
- META-01: title content and length
- META-02: description content and length (150-160 chars)
- META-03: canonical URL exact match
- META-04: theme-color hex value
- SOCIAL-01: all 5 OG tags with values
- SOCIAL-02: twitter:card value
- SOCIAL-03: og-image.png file existence and PNG validity
- SCHEMA-01: JSON-LD parsing, type, properties, pricing
- CRAWL-01: robots.txt content
- CRAWL-02: sitemap.xml content
- CRAWL-03: _headers unset rules
- BRAND-01: favicon files existence, format validity, HTML references
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/seo.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/__tests__/seo.test.ts exists
    - File imports from 'fs', 'path', and 'vitest'
    - File contains describe blocks for: "SEO: Primary Meta Tags", "SEO: Open Graph Tags", "SEO: Twitter Card", "SEO: OG Image Asset", "SEO: JSON-LD Structured Data", "SEO: Crawler Infrastructure", "SEO: Favicon & Branding", "SEO: Anti-patterns excluded"
    - File has test cases referencing all 12 requirement IDs: META-01, META-02, META-03, META-04, SOCIAL-01, SOCIAL-02, SOCIAL-03, SCHEMA-01, CRAWL-01, CRAWL-02, CRAWL-03, BRAND-01
    - `npx vitest run src/__tests__/seo.test.ts` exits with code 0 (all tests pass)
    - Test output shows 0 failed tests
  </acceptance_criteria>
  <done>All 12 SEO requirements have automated test coverage. Test suite passes confirming: correct title/description/canonical/theme-color, all OG and Twitter Card tags present with absolute HTTPS URLs, valid JSON-LD structured data, crawler files exist with correct content, _headers unset rules in place, favicon files valid, no Vite defaults remain.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx vitest run src/__tests__/seo.test.ts` -- all tests pass (0 failures)
2. `npx vitest run` -- full test suite still passes (no regressions)
3. `grep -c 'vite.svg' index.html` returns 0
4. index.html contains all required meta tags, OG tags, Twitter Card, JSON-LD
5. No `<meta name="keywords">` in index.html
</verification>

<success_criteria>
- index.html has complete SEO head: title, description, canonical, theme-color, 5 OG tags, Twitter Card, favicon links, JSON-LD
- No Vite defaults remain (no vite.svg reference, no "sync-multi-cam" slug title)
- SEO test suite exists at src/__tests__/seo.test.ts covering all 12 requirements
- All tests pass: `npx vitest run src/__tests__/seo.test.ts` exits 0
- Full test suite passes: `npx vitest run` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/17-search-discoverability-social-sharing/17-02-SUMMARY.md`
</output>
