---
phase: 17-search-discoverability-social-sharing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/robots.txt
  - public/sitemap.xml
  - public/og-image.png
  - public/favicon.svg
  - public/favicon.ico
  - public/apple-touch-icon.png
  - public/_headers
autonomous: true
requirements:
  - SOCIAL-03
  - CRAWL-01
  - CRAWL-02
  - CRAWL-03
  - BRAND-01

must_haves:
  truths:
    - "robots.txt is served at site root and allows all crawlers"
    - "sitemap.xml is served at site root with the canonical production URL"
    - "OG preview image exists as a 1200x630px PNG at /og-image.png"
    - "Custom favicon files replace the Vite default (SVG, ICO, apple-touch-icon)"
    - "Social crawlers can fetch og-image.png, robots.txt, sitemap.xml without COOP/COEP blocking"
  artifacts:
    - path: "public/robots.txt"
      provides: "Crawler directives and sitemap reference"
      contains: "Sitemap: https://sync-multi-cam.pages.dev/sitemap.xml"
    - path: "public/sitemap.xml"
      provides: "Single-URL sitemap for search engines"
      contains: "<loc>https://sync-multi-cam.pages.dev/</loc>"
    - path: "public/og-image.png"
      provides: "1200x630px social preview placeholder image"
      min_lines: 0
    - path: "public/favicon.svg"
      provides: "SVG favicon with multi-camera grid concept"
      contains: "<svg"
    - path: "public/favicon.ico"
      provides: "ICO favicon for legacy browsers"
      min_lines: 0
    - path: "public/apple-touch-icon.png"
      provides: "180x180 Apple touch icon"
      min_lines: 0
    - path: "public/_headers"
      provides: "COOP/COEP unset rules for SEO asset paths"
      contains: "! Cross-Origin-Embedder-Policy"
  key_links:
    - from: "public/robots.txt"
      to: "public/sitemap.xml"
      via: "Sitemap directive URL"
      pattern: "Sitemap:.*sitemap\\.xml"
    - from: "public/_headers"
      to: "public/og-image.png"
      via: "Path-specific COEP/COOP unset rule"
      pattern: "/og-image\\.png"
---

<objective>
Create all static SEO assets in the public/ directory and update _headers to allow social crawlers to fetch them.

Purpose: Establish the static file foundation (crawler files, social preview image, favicons, header rules) that the index.html meta tags will reference. Social crawlers must be able to fetch og-image.png without COOP/COEP interference.

Output: 6 new files in public/ (robots.txt, sitemap.xml, og-image.png, favicon.svg, favicon.ico, apple-touch-icon.png) and an updated _headers file with path-specific COOP/COEP unset rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-search-discoverability-social-sharing/17-CONTEXT.md
@.planning/phases/17-search-discoverability-social-sharing/17-RESEARCH.md

<interfaces>
<!-- Current state of files being modified -->

From public/_headers (entire file):
```
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
```

From public/ directory listing:
```
_headers
vite.svg
```

Key constants:
- Production URL: https://sync-multi-cam.pages.dev/
- Theme background color: #030712 (Tailwind gray-950)
- OG image dimensions: 1200x630px
- Apple touch icon: 180x180px
- Favicon ICO: 16x16 + 32x32
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create crawler infrastructure and social preview assets</name>
  <files>public/robots.txt, public/sitemap.xml, public/og-image.png, public/favicon.svg, public/favicon.ico, public/apple-touch-icon.png</files>
  <read_first>
    - public/_headers (see current public/ state)
    - .planning/phases/17-search-discoverability-social-sharing/17-RESEARCH.md (Pattern 3 for robots.txt and sitemap.xml exact content)
    - .planning/phases/17-search-discoverability-social-sharing/17-CONTEXT.md (D-04 through D-09 for favicon and og-image decisions)
  </read_first>
  <action>
Create 6 new static files in the `public/` directory:

**1. public/robots.txt** (per D-17, CRAWL-01):
```
User-agent: *
Allow: /

Sitemap: https://sync-multi-cam.pages.dev/sitemap.xml
```
Exactly 4 lines. No Disallow rules. The Sitemap URL must use the production domain exactly.

**2. public/sitemap.xml** (per D-16, D-17, CRAWL-02):
```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://sync-multi-cam.pages.dev/</loc>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
```
The `<loc>` URL must exactly match the canonical URL from D-16.

**3. public/og-image.png** (per D-04, D-05, D-06, SOCIAL-03):
Create a 1200x630px placeholder PNG. Use a Node.js script to generate it programmatically:
- Background: #030712 (app's dark theme, Tailwind gray-950)
- White text centered: "Sync Multi-Cam" (large, bold)
- Smaller text below: "Free Browser-Based Multi-Camera Video Sync"
- Optional: a simple grid/camera icon shape using basic geometry

Use a canvas approach -- create a script that generates the PNG using the `canvas` npm package if available, or create a minimal valid PNG programmatically. If neither works, create the image using an SVG-to-PNG conversion via a one-time script. The file MUST be a valid PNG and MUST be approximately 1200x630 pixels. Keep file size under 300KB.

If programmatic generation is too complex without external tools, create a minimal 1200x630 solid-color PNG as a placeholder. The user will replace it later per D-05.

**4. public/favicon.svg** (per D-07, BRAND-01):
Create an SVG favicon representing a multi-camera grid concept:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" fill="none">
  <!-- 2x2 grid of rounded rectangles representing camera views -->
  <!-- Use app colors: #030712 background, #3b82f6 (blue-500) for grid cells -->
  <rect width="32" height="32" rx="4" fill="#030712"/>
  <rect x="2" y="2" width="13" height="13" rx="2" fill="#3b82f6"/>
  <rect x="17" y="2" width="13" height="13" rx="2" fill="#60a5fa"/>
  <rect x="2" y="17" width="13" height="13" rx="2" fill="#60a5fa"/>
  <rect x="17" y="17" width="13" height="13" rx="2" fill="#3b82f6"/>
</svg>
```
The design should be a 2x2 grid of colored cells on a dark background, representing the multi-camera grid concept. Use slightly different blue shades for visual interest. The viewBox should be 32x32.

**5. public/favicon.ico** (per D-08, BRAND-01):
Generate a multi-size .ico file containing 16x16 and 32x32 versions. Use a Node.js script to convert the SVG to ICO format. If ICO generation is not feasible without external tools, create a minimal valid ICO file. The file MUST be a valid ICO format that browsers can render.

Approach: Write a Node.js script that creates the ICO from raw pixel data matching the SVG grid design. The ICO format is simple: a header followed by BMP or PNG data for each size. Using PNG-in-ICO is the modern approach.

**6. public/apple-touch-icon.png** (per D-08, BRAND-01):
Generate a 180x180px PNG matching the favicon design (2x2 grid on dark background). Use the same approach as the og-image generation.

After creating all files, delete `public/vite.svg` as it is being replaced (per D-09).
  </action>
  <verify>
    <automated>test -f public/robots.txt && test -f public/sitemap.xml && test -f public/og-image.png && test -f public/favicon.svg && test -f public/favicon.ico && test -f public/apple-touch-icon.png && ! test -f public/vite.svg && echo "All 6 assets exist, vite.svg removed"</automated>
  </verify>
  <acceptance_criteria>
    - public/robots.txt exists and contains the exact string "Sitemap: https://sync-multi-cam.pages.dev/sitemap.xml"
    - public/robots.txt contains "User-agent: *" and "Allow: /"
    - public/sitemap.xml exists and contains the exact string "<loc>https://sync-multi-cam.pages.dev/</loc>"
    - public/sitemap.xml starts with "<?xml version"
    - public/og-image.png exists and is a valid PNG (first 4 bytes are 89 50 4E 47)
    - public/favicon.svg exists and contains "<svg" and "viewBox"
    - public/favicon.ico exists and is a valid ICO (first 4 bytes are 00 00 01 00)
    - public/apple-touch-icon.png exists and is a valid PNG
    - public/vite.svg does NOT exist (deleted)
  </acceptance_criteria>
  <done>All 6 static assets created in public/, vite.svg removed. robots.txt allows all crawlers and references sitemap. sitemap.xml has canonical production URL. og-image.png is a 1200x630 placeholder. Favicon set (SVG, ICO, apple-touch-icon) represents multi-camera grid concept.</done>
</task>

<task type="auto">
  <name>Task 2: Update _headers with COOP/COEP unset rules for SEO assets</name>
  <files>public/_headers</files>
  <read_first>
    - public/_headers (current state -- 3 lines with global COOP/COEP rules)
    - .planning/phases/17-search-discoverability-social-sharing/17-RESEARCH.md (Pattern 2 for exact _headers syntax)
    - .planning/phases/17-search-discoverability-social-sharing/17-CONTEXT.md (D-10, D-11, D-12 for header strategy)
  </read_first>
  <action>
Replace the entire `public/_headers` file with the following content (per D-10, D-11, D-12, CRAWL-03):

```
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp

/og-image.png
  ! Cross-Origin-Opener-Policy
  ! Cross-Origin-Embedder-Policy
  Access-Control-Allow-Origin: *

/robots.txt
  ! Cross-Origin-Opener-Policy
  ! Cross-Origin-Embedder-Policy

/sitemap.xml
  ! Cross-Origin-Opener-Policy
  ! Cross-Origin-Embedder-Policy

/favicon.ico
  ! Cross-Origin-Opener-Policy
  ! Cross-Origin-Embedder-Policy

/apple-touch-icon.png
  ! Cross-Origin-Opener-Policy
  ! Cross-Origin-Embedder-Policy

/favicon.svg
  ! Cross-Origin-Opener-Policy
  ! Cross-Origin-Embedder-Policy
```

Key details:
- The `/*` global block MUST remain first (per D-12) -- it sets the default COOP/COEP for the app itself (needed for SharedArrayBuffer/FFmpeg WASM)
- Each SEO asset path gets its own block using Cloudflare Pages `!` unset syntax to remove the inherited COOP/COEP headers
- `/og-image.png` additionally gets `Access-Control-Allow-Origin: *` so social crawlers with CORS-preflight can fetch it
- All 6 paths from D-11 must be present: /og-image.png, /robots.txt, /sitemap.xml, /favicon.ico, /apple-touch-icon.png, /favicon.svg
- Use exactly 2-space indentation for header values (matching Cloudflare Pages convention)
  </action>
  <verify>
    <automated>grep -c "! Cross-Origin-Embedder-Policy" public/_headers | grep -q "6" && grep "og-image.png" public/_headers && grep "Access-Control-Allow-Origin" public/_headers && grep "robots.txt" public/_headers && grep "sitemap.xml" public/_headers && grep "favicon.ico" public/_headers && grep "apple-touch-icon.png" public/_headers && grep "favicon.svg" public/_headers && echo "_headers valid with all 6 unset rules"</automated>
  </verify>
  <acceptance_criteria>
    - public/_headers contains exactly 6 occurrences of "! Cross-Origin-Embedder-Policy" (one per SEO asset path)
    - public/_headers contains exactly 6 occurrences of "! Cross-Origin-Opener-Policy" (one per SEO asset path)
    - public/_headers first non-empty line block starts with "/*" (global rules preserved)
    - public/_headers contains "/og-image.png" followed by "Access-Control-Allow-Origin: *"
    - public/_headers contains all 6 paths: /og-image.png, /robots.txt, /sitemap.xml, /favicon.ico, /apple-touch-icon.png, /favicon.svg
    - The global "Cross-Origin-Opener-Policy: same-origin" and "Cross-Origin-Embedder-Policy: require-corp" lines are still present under "/*"
  </acceptance_criteria>
  <done>_headers file has global COOP/COEP preserved for app functionality and path-specific unset rules for all 6 SEO asset paths. Social crawlers can fetch og-image.png, robots.txt, sitemap.xml, and favicon files without COOP/COEP interference.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 6 new files exist in public/: `ls public/robots.txt public/sitemap.xml public/og-image.png public/favicon.svg public/favicon.ico public/apple-touch-icon.png`
2. vite.svg is gone: `! test -f public/vite.svg`
3. _headers has 6 unset blocks: `grep -c "! Cross-Origin-Embedder-Policy" public/_headers` returns 6
4. robots.txt references sitemap: `grep "Sitemap:" public/robots.txt`
5. sitemap.xml has canonical URL: `grep "sync-multi-cam.pages.dev" public/sitemap.xml`
</verification>

<success_criteria>
- 6 new static files created in public/ (robots.txt, sitemap.xml, og-image.png, favicon.svg, favicon.ico, apple-touch-icon.png)
- vite.svg deleted from public/
- _headers updated with 6 path-specific COOP/COEP unset rules plus CORS on og-image
- All files are valid formats (PNG header bytes, ICO header bytes, well-formed XML, valid SVG)
- Production URLs used consistently: https://sync-multi-cam.pages.dev/
</success_criteria>

<output>
After completion, create `.planning/phases/17-search-discoverability-social-sharing/17-01-SUMMARY.md`
</output>
