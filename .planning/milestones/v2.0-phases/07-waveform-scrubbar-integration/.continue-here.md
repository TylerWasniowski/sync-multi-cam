---
phase: 07-waveform-scrubbar-integration
task: 2
total_tasks: 2
plan: 07-02
status: in_progress
last_updated: "2026-03-03T17:12:50.763Z"
---

<current_state>
Implemented offset-based shared timeline: waveforms shift by offsetSeconds, playback uses original files with offset-based sync, pre-offset videos show black. All 4 files modified and TypeScript compiles clean. One remaining bug: when the leader video ends, `handleEnded` calls `updateVideoVisibility(Infinity)` which makes ALL videos go black — but the leader should stay on its last frame (only followers that ended earlier should go black during playback).

This is NOT a Phase 7 task — it's a bugfix from the offset-based timeline refactor done in this session. Phase 7's original 07-02 human-verify checkpoint is still pending after this fix.
</current_state>

<completed_work>

### Offset-based shared timeline (new work this session)
- WaveformCanvas.tsx: Shifted waveform drawing by `offsetSamples`, removed trim overlay, adjusted drawTrackEnd for shared timeline
- WaveformPanel.tsx: Updated `maxTotalSamples` to account for `offset + duration` per track
- PlaybackSection.tsx: Switched from trimSeconds-based to offsetSeconds-based playback model:
  - Leader = reference track (isReference), followers use `-offsetSeconds` offsets
  - Timeline starts at `maxOffset` (sync point where all cameras overlap)
  - Video visibility via `opacity` — before offset or after duration = black
  - Seek/scrub use shared timeline time directly (no trimSeconds conversion)
  - Poster extraction uses `time - offsetSeconds` per track
- videoSync.ts: Added pre-offset follower handling (pause when expectedTime < 0, auto-start when crossing into active) and post-duration follower handling (pause when expectedTime >= duration)

### Previous session work (already committed)
- Plan 07-01: Complete (3/3 tasks) — scrub lifecycle, waveform interaction inversion, playhead
- Plan 07-02 Task 1: Complete — playhead follow mode, zoom anchoring (commit 2a232e4)
</completed_work>

<remaining_work>

1. **Fix end-of-playback visibility bug**: In PlaybackSection.tsx `handleEnded`, remove `updateVideoVisibility(Infinity)` — it makes all videos black at end of playback. Instead, only hide followers whose `expectedTime >= follower.duration` during the sync loop (already handled in videoSync.ts). The leader should just show its last frame when playback ends naturally. The `updateVideoVisibility` call in onFrame already handles mid-playback follower endings correctly.

2. **Complete 07-02 human-verify checkpoint**: Run through the 12-step verification checklist from 07-02-PLAN.md with the offset-based timeline changes. The checklist covers: playhead visibility, click-to-seek, drag-to-scrub, scrub-resume, Shift+drag pan, UI hints, cursor styles, auto-follow, zoom anchoring.

3. **After 07-02 completes**: Phase verification (gsd-verifier), create 07-02-SUMMARY.md, roadmap update, state update.
</remaining_work>

<decisions_made>

- Offset-based shared timeline instead of trimSeconds labels: User preferred shifting waveforms to start at their actual offset position rather than changing labels to show trim amounts
- Start at sync point (maxOffset): User chose to start playback at the point where all cameras overlap, not at timeline 0
- Video visibility via CSS opacity: Using `video.style.opacity = '0'/'1'` with VideoGrid's `bg-black` showing through — no component API changes needed, performant for per-frame updates
- Sync engine handles pre-offset AND post-duration: Followers are paused when outside their active range, auto-started when entering range
- Original files used for playback: Videos already loaded from `result.originalFile`, just changed the offset model from trimSeconds to offsetSeconds
</decisions_made>

<blockers>
- End-of-playback visibility bug: `updateVideoVisibility(Infinity)` in handleEnded makes ALL videos black. Simple fix: just remove that line. The sync engine's onFrame callback already handles hiding followers that end before the leader. When the leader naturally ends, playback stops and all videos show their last frame — which is correct behavior.
</blockers>

<context>
The offset-based timeline is a significant improvement over the original trimSeconds model. It fixed a pre-existing sync issue the user noticed in playback (trimSeconds had keyframe-snapping imprecision), and the waveform offset labels (+X.XXs) now make visual sense because the waveform literally starts at that position.

The fix for the end-of-playback bug is a one-liner: remove `updateVideoVisibility(Infinity)` from handleEnded (PlaybackSection.tsx ~line 213). The leader ending is the natural end of playback — videos should show their last frame, same as any video player. Mid-playback follower endings are already handled correctly by the onFrame callback.

Files modified (uncommitted):
- src/components/WaveformCanvas.tsx
- src/components/WaveformPanel.tsx
- src/components/PlaybackSection.tsx
- src/lib/videoSync.ts
</context>

<next_action>
1. Fix the one-liner bug: remove `updateVideoVisibility(Infinity)` from `handleEnded` in PlaybackSection.tsx (~line 213)
2. Restart dev server (`npm run dev`) and test:
   - Seek before a follower's offset → that tile goes black ✓
   - Play through a follower's end → that tile goes black mid-playback ✓
   - Play to end of leader → all videos show last frame (NOT black) ✓
3. If all passes, proceed to 07-02 human-verify checkpoint
4. After verification: `/gsd:execute-phase 07` to complete the phase
</next_action>
